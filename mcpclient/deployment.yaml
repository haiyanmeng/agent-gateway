apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-agent-deployment
  labels:
    app: mcp-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-agent
  template:
    metadata:
      labels:
        app: mcp-agent
    spec:
      containers:
      - name: mcp-agent
        image: docker.io/hmeng/agent:v6
        imagePullPolicy: Always
        env:
        - name: MCP_SERVER_URL
          value: "http://envoy-service:10001/mcp-server1/mcp"
        command: ["/bin/sh", "-c", "python agent.py && sleep infinity"]
        # Add resource limits and requests as appropriate for your environment
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "250m"
      serviceAccountName: sa1
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sa1
